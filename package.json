process.on('uncaughtException', (err) => {
    console.error('CRASH LOG:', err);
    process.exit(1);
});

const express = require('express');
const http = require('http');
// ... các dòng code tiếp theo giữ nguyên ...

{
  "name": "mc-afk-bot",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "bedrock-protocol": "^3.12.0",
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  }
}
